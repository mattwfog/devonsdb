# Fuzz findings

Engine findings from local runs belong here. A finding records the target,
seed, exact input bytes (hex and the matching corpus path), panic or signal,
and the complete backtrace. Engine fixes are intentionally outside this
tool's ownership.

## Current findings

None.

